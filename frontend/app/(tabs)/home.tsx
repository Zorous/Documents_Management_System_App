import React from 'react';
import { StyleSheet, View, Text } from 'react-native';

const Home = () => {
    return (
        <View>
            <Text>Homeexe</Text>
        </View>
    );
}

const styles = StyleSheet.create({})

export default Home;
